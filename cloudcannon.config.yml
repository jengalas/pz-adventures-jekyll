_select_data:
  mark_types:
    - Survey Mark
    - Reference Mark

collections_config:
  posts:
    schemas:
      default:
        path: schemas/post.md
  geocaches:
    schemas:
      default:
        path: schemas/geocache.md